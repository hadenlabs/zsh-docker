# Docker Compose
# https://docs.docker.com/compose/
#
# Usage: test up

version: "3"
services:
  app:
    image: zsh-docker-aliases-app-test:0.0.2
    environment: &environment
      - STAGE=testing
    build:
      args:
        stage: test
        version: 0.0.2
